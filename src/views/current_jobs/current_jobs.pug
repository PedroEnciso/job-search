h2.prose.prose-2xl.text-center.mb-6 Your current openings
if error
  p.text-center= error
else
  include filter_buttons.pug 
  ul.flex.flex-col.max-w-2xl.mx-auto.gap-8
    each job in jobs 
      include job.pug
