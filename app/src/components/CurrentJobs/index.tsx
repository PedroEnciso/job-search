// import type { CurrentJob } from "../../../../server/types";

function CurrentJobsSection() {
  return <section>current jobs</section>;
}

export default CurrentJobsSection;

// interface CurrentJobsSectionProps {
//   is_pending: boolean;
//   error: Error;
//   current_jobs: CurrentJob;
// }
